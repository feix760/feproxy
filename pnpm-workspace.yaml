# pnpm 11 blocks dependency build scripts by default: anything not declared here exits with
# ERR_PNPM_IGNORED_BUILDS. unrs-resolver comes from eslint-import-resolver-typescript, and its
# postinstall only checks the napi platform packages.
allowBuilds:
  unrs-resolver: true
